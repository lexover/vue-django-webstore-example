version: "3.3"
services:
  redis:
    image: "redis:alpine"
  
  web: 
    build: .
    ports:
      - 8080:8080
  
  celery: 
    build: .
    command: celery -A drugstore worker --loglevel=INFO --logfile=logs/celery.log
    depends_on: 
      - web 
      - redis
    links:
      - redis